<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <corners android:radius="150dp" /> <!-- Ajuste le rayon pour des coins plus ou moins arrondis -->
            <solid android:color="#2FDCDC" /> <!-- Couleur de l'arrière-plan -->
        </shape>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape android:shape="rectangle">
                <corners android:radius="150dp" />
                <solid android:color="@color/white" /> <!-- Couleur de la partie remplie de la SeekBar -->
            </shape>
        </clip>
    </item>
</layer-list>
